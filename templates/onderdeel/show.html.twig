{% extends 'base.html.twig' %}

{% block title %}Onderdeel{% endblock %}

{% block body %}
    <h1>Onderdeel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ onderdeel.id }}</td>
            </tr>
            <tr>
                <th>Naam</th>
                <td>{{ onderdeel.naam }}</td>
            </tr>
            <tr>
                <th>Omschrijving</th>
                <td>{{ onderdeel.omschrijving }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('onderdeel_index') }}">back to list</a>

    <a href="{{ path('onderdeel_edit', {'id': onderdeel.id}) }}">edit</a>

    {{ include('onderdeel/_delete_form.html.twig') }}
{% endblock %}
